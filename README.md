# Универсальный Справочник Инфраструктуры

> **Источник правды** для всей инфраструктуры проекта. Любое изменение → правка карточки в течение суток.

## Оглавление

1. [Регламент](#регламент)
2. [Паспорт хоста](#паспорт-хоста)
3. [Каталог контейнеров](#каталог-контейнеров)
4. [Каталог сервисов/ботов](#каталог-сервисовботов)
5. [Шаблоны карточек](#шаблоны-карточек)
6. [Глоссарий](#глоссарий)

## Дополнительная документация

- 📋 [Быстрый старт](docs/quick-start.md) — руководство по началу работы
- 🔐 [Миграция секретов](docs/credentials-migration.md) — план переноса паролей в Vault
- 🗺️ [Дорожная карта](docs/infrastructure-roadmap.md) — план развития инфраструктуры
- 📝 [История изменений](docs/changelog.md) — changelog проекта

---

## Регламент

### Общие правила

- **Источник правды — этот справочник.** Любое изменение в инфраструктуре → правка соответствующей карточки в течение суток.
- **Один объект — одна карточка.** Хост/контейнер/сервис описываются отдельно.
- **Runbook хранится в карточке сервиса** (раздел «Операции»), кратко и пошагово.
- **Секреты не записывать.** Только `vault://…` или «см. хранитель секретов».
- **Команды давать проверенные**, по шагам, с кратким «зачем».
- **Минимум форматирования:** заголовки, списки, код-блоки; без таблиц, если можно списком.
- **Обновляемость:** при изменении портов, путей, доменов, зависимостей — правка сразу же.
- **Единый нейминг:** `lxc-<id>-<краткое-имя>`; сервисы — `<stack>-<роль>` (пример: `n8n-core`).

### Форматирование

- **Формат:** простой текст/Markdown (заголовки, списки, кодовые блоки для команд)
- **Язык:** русский, **команды** — латиницей, без кириллицы
- **Лаконичность:** списки вместо прозы; 1–2 строки на пункт
- **Никаких секретов** (паролей, токенов). Только ссылки-указатели вида: `vault://путь/к/секрету`
- **Даты:** `YYYY-MM-DD`. Часовой пояс не указывать

---

## Паспорт хоста

### Хост — Proxmox VE (pve)

**Назначение:** Основной хост виртуализации для продакшн-среды, разворачивание LXC контейнеров для веб-сервисов, баз данных и ботов

**Железо:** 
- CPU: AMD FX(tm)-6300 Six-Core Processor (6 ядер, 3.5GHz)
- RAM: 14GB (13Gi доступно)
- Материнка: Gigabyte 970A-DS3P
- GPU: AMD Radeon RX 460/560D (Baffin)
- Диски: 
  - sda: 3.6TB HDD (filestorage)
  - sdb: 931.5GB SSD (fastpool ZFS)
  - sdc: 223.6GB SSD (система + local-lvm)
  - sdd: 931.5GB SSD (fastpool ZFS)

**Сеть:** 
- Bridge: vmbr0 (основной)
- LAN IP: 192.168.1.253/24
- Внешний доступ: AT&T модем, проброшены TCP 80/443
- DNS: dnsmasq (split-DNS для внутренних доменов)

**Хранилища:** 
- local: /var/lib/vz (57GB, системные файлы PVE)
- local-lvm: thin LVM на sdc3 (130GB, тестовые ВМ)
- fastpool: ZFS RAID-0 на sdb1+sdd1 (927GB, основные диски ВМ/LXC)
- filestorage: /mnt/storage на sda1 (3.36TB, ISO, шаблоны, бэкапы)
- tplpool: /fastpool/lxc-templates (927GB, отдельный склад шаблонов)

**Бэкапы VM/LXC:** 
- Инструмент: vzdump (встроенный)
- PostgreSQL: ежедневный logical dump (7 дней)
- Docker volumes: еженедельные snapshots
- Конфигурации: git репозитории

**Обслуживание:** 
- Обновления PVE: по мере необходимости
- Мониторинг: ручная проверка через веб-интерфейс
- Сертификаты: автообновление через certbot

**Известные грабли:**
- Hair-pin NAT отсутствует — решено через dnsmasq split-DNS
- При обновлении ядра могут не подниматься сетевые интерфейсы
- Postgres в хосте мешал контейнерам — удалили пакеты
- listen_addresses требует restart вместо reload
- SECURE_COOKIE=true требует HTTPS — изменили на false для HTTP

**Обновлено:** 2024-12-19

---

## Каталог контейнеров

> Здесь размещаются карточки всех контейнеров и ВМ. Каждый контейнер/ВМ получает отдельную карточку по шаблону.

### lxc-110-databases

**Роль:** Сервер баз данных PostgreSQL для всех сервисов

**Расположение:** 
- Хост: Proxmox VE (pve)
- IP: 192.168.1.110/24

**Ресурсы:** 
- CPU: настроено в PVE
- RAM: настроено в PVE
- Disk: хранилище tppool

**Сеть/Порты:** 
- 5432 → 5432 (PostgreSQL)
- 22 → SSH

**Данные/тома:** 
- `/var/lib/postgresql/17/main` — данные PostgreSQL
- `/etc/postgresql/17/main` — конфигурация PostgreSQL

**Сервисы внутри:** 
- postgresql-17
- postgresql-client-17

**Бэкап контейнера:** 
- Proxmox snapshot: pg_initial_setup
- Ежедневный logical dump через cron (7 дней)

**Известные грабли:**
- listen_addresses требует restart вместо reload
- Проблемы прав владельца после pg_restore — исправлять через ALTER OWNER и GRANT

**Обновлено:** 2024-12-19

---

### lxc-120-apps

**Роль:** Контейнер для Docker-приложений (n8n, боты, Kimai, Taiga)

**Расположение:** 
- Хост: Proxmox VE (pve)
- IP: 192.168.1.120/24

**Ресурсы:** 
- CPU: настроено в PVE
- RAM: настроено в PVE
- Disk: хранилище tppool
- Доп-фичи: nesting=1, keyctl=1, unprivileged

**Сеть/Порты:** 
- 5678 → 5678 (n8n)
- 8080 → 8001 (Kimai)
- 9000 → 80 (Taiga)
- 3333 → 3333 (n8n-mcp HTTP)
- 22 → SSH

**Данные/тома:** 
- `/opt/kimai` — Kimai docker-compose стек
- `/opt/taiga-full` — Taiga docker-compose стек
- `/opt/n8n-mcp` — n8n MCP bridge скрипты
- `/root/*.env` — переменные окружения для ботов
- Docker volumes: n8n_data, kimai_files, taiga-*

**Сервисы внутри:** 
- Docker Engine 28.03
- n8n (контейнер)
- Kimai + MariaDB (контейнеры)
- Taiga + PostgreSQL + RabbitMQ (контейнеры)
- Telegram-боты: German, Cardio, Mamontov (контейнеры)

**Бэкап контейнера:** 
- Proxmox snapshot: n8n_ok_http5678
- Docker volumes: еженедельные snapshots
- Конфигурации в git репозиториях

**Известные грабли:**
- Docker-порт проброшен неверно — проверять порты в docker run
- Отсутствующие .env файлы — проверять перед запуском
- 502 Bad Gateway после рестарта — перезапускать nginx-gateway в Taiga

**Обновлено:** 2024-12-19

---

### lxc-130-service

**Роль:** Дополнительный сервисный контейнер

**Расположение:** 
- Хост: Proxmox VE (pve)
- IP: 192.168.1.130/24

**Ресурсы:** 
- CPU: настроено в PVE
- RAM: настроено в PVE
- Disk: хранилище tppool

**Сеть/Порты:** 
- 22 → SSH

**Данные/тома:** 
- N/A

**Сервисы внутри:** 
- базовая система Debian

**Бэкап контейнера:** Proxmox vzdump по расписанию

**Известные грабли:**
- N/A

**Обновлено:** 2024-12-19

---

### lxc-140-gigabot

**Роль:** Контейнер для GigaBot

**Расположение:** 
- Хост: Proxmox VE (pve)
- IP: 192.168.1.140/24

**Ресурсы:** 
- CPU: настроено в PVE
- RAM: настроено в PVE
- Disk: хранилище tppool

**Сеть/Порты:** 
- 22 → SSH

**Данные/тома:** 
- конфигурация и данные GigaBot

**Сервисы внутри:** 
- GigaBot

**Бэкап контейнера:** Proxmox vzdump по расписанию

**Известные грабли:**
- N/A

**Обновлено:** 2024-12-19

---

### lxc-150-mcp-taiga

**Роль:** Taiga MCP Bridge для интеграции с Claude

**Расположение:** 
- Хост: Proxmox VE (pve)
- IP: 192.168.1.150/24

**Ресурсы:** 
- CPU: настроено в PVE
- RAM: настроено в PVE
- Disk: хранилище tppool

**Сеть/Порты:** 
- 8000 → 8000 (HTTP-SSE сервер)
- 22 → SSH

**Данные/тома:** 
- `/opt/pytaiga-mcp` — проект MCP bridge
- `/opt/pytaiga-mcp/.venv` — виртуальное окружение Python
- `/opt/pytaiga-mcp/.env` — конфигурация

**Сервисы внутри:** 
- pytaiga-mcp (Python HTTP-SSE сервер)
- systemd служба taiga-mcp (опционально)

**Бэкап контейнера:** 
- git репозиторий проекта
- backup конфигурации .env

**Известные грабли:**
- "Тишина" в консоли после запуска — установить LOG_FILE=/dev/stdout
- HTTP 307 при обращении к /sse/ — использовать /sse без слэша
- Проверка работоспособности: curl -v -N http://192.168.1.150:8000/sse

**Обновлено:** 2024-12-19

---

### lxc-160-cloud

**Роль:** Nextcloud сервер для файлового хранилища

**Расположение:** 
- Хост: Proxmox VE (pve)
- IP: 192.168.1.160/24

**Ресурсы:** 
- CPU: настроено в PVE
- RAM: настроено в PVE
- Disk: хранилище tppool

**Сеть/Порты:** 
- 80 → 80 (HTTP)
- 443 → 443 (HTTPS)
- 22 → SSH

**Данные/тома:** 
- Nextcloud данные и конфигурация
- MariaDB данные

**Сервисы внутри:** 
- Nextcloud
- MariaDB

**Бэкап контейнера:** Proxmox vzdump по расписанию

**Известные грабли:**
- N/A

**Обновлено:** 2024-12-19

---

## Каталог сервисов/ботов

> Здесь размещаются карточки всех сервисов, ботов и микросервисов. Каждый сервис получает отдельную карточку по шаблону.

### postgresql-main

**Назначение:** Основная база данных PostgreSQL 17 для всех сервисов проекта

**Расположение:** 
- Контейнер: lxc-110-databases
- IP: 192.168.1.110:5432

**Интерфейсы/Порты:** 
- 5432/TCP — подключения к БД из сети 192.168.1.0/24

**Данные:** 
- Данные: `/var/lib/postgresql/17/main`
- Конфигурация: `/etc/postgresql/17/main/postgresql.conf`
- Аутентификация: `/etc/postgresql/17/main/pg_hba.conf`

**Зависимости:** 
- Нет внешних зависимостей

**Конфигурация/переменные:** 
- listen_addresses = '192.168.1.110,localhost'
- pg_hba: host all all 192.168.1.0/24 scram-sha-256
- Секреты: vault://databases/postgresql (пароли пользователей)

**Мониторинг/здоровье:** 
- Проверка: `pg_isready -h 192.168.1.110`
- Статус: `systemctl status postgresql`

## Операции

### Обновление
1. Создать logical dump: `pg_dumpall -h 192.168.1.110 > backup_$(date +%Y%m%d).sql`
2. Остановить сервис: `systemctl stop postgresql`
3. Обновить пакет: `apt update && apt upgrade postgresql-17`
4. Запустить сервис: `systemctl start postgresql`
5. Проверить подключение: `pg_isready -h 192.168.1.110`

### Бэкап/восстановление
1. Logical dump всех БД: `pg_dumpall -h 192.168.1.110 > full_backup_$(date +%Y%m%d).sql`
2. Dump отдельной БД: `pg_dump -h 192.168.1.110 dbname > dbname_$(date +%Y%m%d).sql`
3. Восстановление: `psql -h 192.168.1.110 < backup_file.sql`
4. Проверить права: `\du` и `\l` в psql
5. Исправить владельцев: `ALTER DATABASE dbname OWNER TO username`

### Проверка после действий
- Все базы данных доступны и отвечают
- Подключения от сервисов работают корректно
- Логи не содержат ошибок подключения
- Права доступа настроены правильно

**Известные грабли:**
- listen_addresses требует restart вместо reload
- Проблемы прав владельца после pg_restore — исправлять через ALTER OWNER и GRANT
- Firewall должен разрешать порт 5432 из сети 192.168.1.0/24

**Обновлено:** 2024-12-19

---

### nginx-proxy

**Назначение:** Reverse-proxy на хосте PVE для маршрутизации внешнего трафика к сервисам

**Расположение:** 
- Контейнер: хост PVE (192.168.1.253)
- Домены: pve.gramini.org, n8n.gramini.org

**Интерфейсы/Порты:** 
- 80/HTTP → автоматическое перенаправление на HTTPS
- 443/HTTPS → основные vhost'ы

**Данные:** 
- Конфигурация: `/etc/nginx/sites-available/`
- SSL сертификаты: `/etc/letsencrypt/live/*/fullchain.pem`
- Логи: `/var/log/nginx/`

**Зависимости:** 
- certbot (Let's Encrypt сертификаты)
- dnsmasq (split-DNS)

**Конфигурация/переменные:** 
- pve.gramini.org → 127.0.0.1:8006 (Proxmox админка)
- n8n.gramini.org → 192.168.1.120:5678 (n8n)
- Секреты: N/A

**Мониторинг/здоровье:** 
- Проверка: `curl -I https://pve.gramini.org`
- Статус: `systemctl status nginx`

## Операции

### Обновление
1. Создать бэкап конфигурации: `cp -r /etc/nginx /etc/nginx.backup.$(date +%Y%m%d)`
2. Обновить пакет: `apt update && apt upgrade nginx`
3. Проверить конфигурацию: `nginx -t`
4. Перезагрузить конфигурацию: `systemctl reload nginx`
5. Проверить доступность: `curl -I https://pve.gramini.org`

### Бэкап/восстановление
1. Бэкап конфигурации: `tar -czf nginx-config-$(date +%Y%m%d).tar.gz /etc/nginx`
2. Бэкап SSL сертификатов: `tar -czf ssl-certs-$(date +%Y%m%d).tar.gz /etc/letsencrypt`
3. Восстановление конфигурации: `tar -xzf nginx-config-YYYYMMDD.tar.gz -C /`
4. Проверить конфигурацию: `nginx -t`
5. Перезапустить: `systemctl restart nginx`

### Проверка после действий
- HTTPS статус 200 на всех vhost'ах
- SSL сертификаты валидны и не истекли
- Проксирование на upstream сервисы работает
- Логи не содержат критических ошибок

**Известные грабли:**
- Hair-pin NAT отсутствует — решено через dnsmasq split-DNS
- Certbot ошибки NXDOMAIN — проверить DNS и порты
- Путь к сертификату должен быть fullchain.pem, не cert.pem

**Обновлено:** 2024-12-19

---

### n8n-automation

**Назначение:** Платформа автоматизации workflow'ов и интеграций

**Расположение:** 
- Контейнер: lxc-120-apps
- URL: https://n8n.gramini.org (внешний), http://192.168.1.120:5678 (внутренний)

**Интерфейсы/Порты:** 
- 5678/HTTP — веб-интерфейс n8n
- 3333/HTTP — n8n MCP bridge API

**Данные:** 
- Docker volume: n8n_data → /home/node/.n8n
- Конфигурация: переменные окружения в docker run
- Workflow'ы и учётные данные в БД

**Зависимости:** 
- PostgreSQL (192.168.1.110:5432, БД n8n)
- nginx-proxy (для внешнего доступа)

**Конфигурация/переменные:** 
- DB_TYPE=postgresdb
- DB_POSTGRESDB_HOST=192.168.1.110
- N8N_EDITOR_BASE_URL=https://n8n.gramini.org
- Секреты: vault://n8n/credentials

**Мониторинг/здоровье:** 
- Проверка: `curl -f http://192.168.1.120:5678/healthz`
- Docker статус: `docker ps | grep n8n`

## Операции

### Обновление
1. Создать бэкап БД: `pg_dump -h 192.168.1.110 n8n > n8n_backup_$(date +%Y%m%d).sql`
2. Остановить контейнер: `docker stop n8n`
3. Удалить старый контейнер: `docker rm n8n`
4. Запустить новую версию: `docker run -d --name n8n ... n8nio/n8n:latest`
5. Проверить доступность: `curl -f http://192.168.1.120:5678/healthz`

### Бэкап/восстановление
1. Остановить контейнер: `docker stop n8n`
2. Бэкап volume: `docker run --rm -v n8n_data:/data -v $(pwd):/backup alpine tar czf /backup/n8n_data_$(date +%Y%m%d).tar.gz /data`
3. Бэкап БД: `pg_dump -h 192.168.1.110 n8n > n8n_db_$(date +%Y%m%d).sql`
4. Восстановление volume: `docker run --rm -v n8n_data:/data -v $(pwd):/backup alpine tar xzf /backup/n8n_data_YYYYMMDD.tar.gz -C /`
5. Восстановление БД: `psql -h 192.168.1.110 n8n < n8n_db_YYYYMMDD.sql`

### Проверка после действий
- Веб-интерфейс доступен и отвечает
- Подключение к PostgreSQL работает
- Активные workflow'ы выполняются
- MCP bridge API отвечает на порту 3333

**Известные грабли:**
- Docker-порт проброшен неверно — проверять 5678:5678 в docker run
- SECURE_COOKIE=true требует HTTPS — установлено false для HTTP
- Basic Auth: admin / vault://n8n/admin-password

**Обновлено:** 2024-12-19

---

### kimai-timetracker

**Назначение:** Система учёта рабочего времени и тайм-трекинг для проектов

**Расположение:** 
- Контейнер: lxc-120-apps
- URL: http://192.168.1.120:8080 (внутренний)

**Интерфейсы/Порты:** 
- 8080/HTTP → 8001 (Kimai веб-интерфейс)

**Данные:** 
- Docker compose: `/opt/kimai/docker-compose.yml`
- MariaDB данные: volume db_data
- Файлы Kimai: volume kimai_files → /opt/kimai/var/data

**Зависимости:** 
- MariaDB 11 (внутри Docker stack)

**Конфигурация/переменные:** 
- DATABASE_URL=mysql://kimai:password@db/kimai
- APP_ENV=prod
- Секреты: vault://kimai/credentials

**Мониторинг/здоровье:** 
- Проверка: `curl -f http://192.168.1.120:8080`
- Docker статус: `docker compose ps` в /opt/kimai

## Операции

### Обновление
1. Создать бэкап БД: `docker compose exec db mysqldump -u root -p kimai > kimai_backup_$(date +%Y%m%d).sql`
2. Остановить стек: `cd /opt/kimai && docker compose down`
3. Обновить образы: `docker compose pull`
4. Запустить стек: `docker compose up -d`
5. Проверить доступность: `curl -f http://192.168.1.120:8080`

### Бэкап/восстановление
1. Остановить стек: `cd /opt/kimai && docker compose down`
2. Бэкап volumes: `docker run --rm -v kimai_db_data:/data -v $(pwd):/backup alpine tar czf /backup/kimai_db_$(date +%Y%m%d).tar.gz /data`
3. Бэкап файлов: `docker run --rm -v kimai_kimai_files:/data -v $(pwd):/backup alpine tar czf /backup/kimai_files_$(date +%Y%m%d).tar.gz /data`
4. Восстановление: обратная операция с tar xzf
5. Запустить стек: `docker compose up -d`

### Проверка после действий
- Веб-интерфейс доступен и отвечает
- Подключение к MariaDB работает
- Данные проектов и времени сохранены
- Вложения и отчёты доступны

**Известные грабли:**
- Порт 8080:8001 в docker-compose — проверять маппинг
- MariaDB пароли в docker-compose.yml — использовать vault://

**Обновлено:** 2024-12-19

---

### taiga-projectmanager

**Назначение:** Система управления проектами и задачами (Agile/Scrum)

**Расположение:** 
- Контейнер: lxc-120-apps
- URL: http://192.168.1.120:9000 (внутренний)

**Интерфейсы/Порты:** 
- 9000/HTTP → 80 (Taiga веб-интерфейс через nginx-gateway)
- 8000/HTTP — REST API (внутренний)
- 8888/HTTP — WebSocket events (внутренний)

**Данные:** 
- Docker compose: `/opt/taiga-full/docker-compose.yml`
- Конфигурация: `/opt/taiga-full/.env`
- PostgreSQL данные: volume taiga-db-data
- Статика: volume taiga-static-data
- Медиа: volume taiga-media-data

**Зависимости:** 
- PostgreSQL 12 (внутри Docker stack)
- RabbitMQ (2 экземпляра для async и events)
- Nginx gateway (внутри stack)

**Конфигурация/переменные:** 
- TAIGA_DOMAIN=192.168.1.120:9000
- POSTGRES_HOST=taiga-db
- EMAIL_HOST=smtp.gmail.com
- Секреты: vault://taiga/credentials

**Мониторинг/здоровье:** 
- Проверка: `curl -f http://192.168.1.120:9000`
- Docker статус: `docker compose ps` в /opt/taiga-full

## Операции

### Обновление
1. Создать бэкап БД: `cd /opt/taiga-full && docker compose exec taiga-db pg_dump -U taiga taiga > taiga_backup_$(date +%Y%m%d).sql`
2. Остановить стек: `docker compose down`
3. Обновить образы: `docker compose pull`
4. Запустить стек: `docker compose up -d`
5. Проверить доступность: `curl -f http://192.168.1.120:9000`

### Бэкап/восстановление
1. Остановить стек: `cd /opt/taiga-full && docker compose down`
2. Бэкап БД: `docker run --rm -v taiga-full_taiga-db-data:/data -v $(pwd):/backup alpine tar czf /backup/taiga_db_$(date +%Y%m%d).tar.gz /data`
3. Бэкап статики: `docker run --rm -v taiga-full_taiga-static-data:/data -v $(pwd):/backup alpine tar czf /backup/taiga_static_$(date +%Y%m%d).tar.gz /data`
4. Бэкап медиа: `docker run --rm -v taiga-full_taiga-media-data:/data -v $(pwd):/backup alpine tar czf /backup/taiga_media_$(date +%Y%m%d).tar.gz /data`
5. Восстановление: обратная операция с tar xzf

### Проверка после действий
- Веб-интерфейс доступен и отвечает
- Подключение к PostgreSQL работает
- WebSocket события работают
- Проекты и задачи отображаются корректно

**Известные грабли:**
- 502 Bad Gateway после рестарта taiga-back — перезапустить taiga-gateway
- 403 ACCESS_REFUSED в RabbitMQ — обновить пароли в контейнерах
- Статика не обновляется — удалить том taiga-static-data и пересоздать

**Обновлено:** 2024-12-19

---

### telegram-bots

**Назначение:** Telegram боты для различных проектов (German, Cardio, Mamontov)

**Расположение:** 
- Контейнер: lxc-120-apps
- Боты работают через Docker контейнеры

**Интерфейсы/Порты:** 
- Исходящие HTTPS подключения к Telegram API
- Webhook endpoints (если используются)

**Данные:** 
- Конфигурация: `/root/*.env` файлы
- Данные ботов в соответствующих БД PostgreSQL

**Зависимости:** 
- PostgreSQL (192.168.1.110) — БД germanbot, cardiobot, mamontov
- Telegram Bot API
- GitHub (для auto-deploy через PAT)

**Конфигурация/переменные:** 
- Переменные окружения в /root/german.env, /root/cardio.env, /root/mamontov.env
- Секреты: vault://telegram-bots/tokens

**Мониторинг/здоровье:** 
- Docker статус: `docker ps | grep bot`
- Проверка логов: `docker logs bot_name`

## Операции

### Обновление
1. Создать бэкап БД ботов: `pg_dump -h 192.168.1.110 germanbot > germanbot_backup_$(date +%Y%m%d).sql`
2. Остановить контейнеры ботов: `docker stop german_bot cardio_bot mamontov_bot`
3. Обновить образы: `docker pull latest_image`
4. Запустить обновлённые контейнеры
5. Проверить логи: `docker logs bot_name`

### Бэкап/восстановление
1. Бэкап БД каждого бота: `pg_dump -h 192.168.1.110 botname > botname_$(date +%Y%m%d).sql`
2. Бэкап конфигураций: `cp /root/*.env /backup/`
3. Восстановление БД: `psql -h 192.168.1.110 botname < botname_backup.sql`
4. Восстановление конфигураций: `cp /backup/*.env /root/`
5. Перезапустить ботов

### Проверка после действий
- Боты отвечают на команды в Telegram
- Подключение к PostgreSQL работает
- Логи не содержат критических ошибок
- Auto-deploy работает при push в GitHub

**Известные грабли:**
- Отсутствующие .env файлы — проверять перед запуском Docker
- GitHub PAT токены могут истекать — обновлять в vault
- Telegram API rate limits — добавлены задержки в коде

**Обновлено:** 2024-12-19

---

### taiga-mcp-bridge

**Назначение:** MCP (Model Context Protocol) мост для интеграции Taiga с Claude AI

**Расположение:** 
- Контейнер: lxc-150-mcp-taiga
- URL: http://192.168.1.150:8000/sse (HTTP-SSE endpoint)

**Интерфейсы/Порты:** 
- 8000/HTTP — SSE сервер для MCP коммуникации

**Данные:** 
- Проект: `/opt/pytaiga-mcp`
- Виртуальное окружение: `/opt/pytaiga-mcp/.venv`
- Конфигурация: `/opt/pytaiga-mcp/.env`
- Логи: stdout (LOG_FILE=/dev/stdout)

**Зависимости:** 
- Taiga API (http://192.168.1.120:9000/api/v1)
- Python 3.x + виртуальное окружение

**Конфигурация/переменные:** 
- TAIGA_API_URL=http://192.168.1.120:9000/api/v1
- TAIGA_TRANSPORT=sse
- TAIGA_USERNAME=master
- Секреты: vault://taiga-mcp/credentials

**Мониторинг/здоровье:** 
- Проверка: `curl -v -N http://192.168.1.150:8000/sse`
- Статус: `systemctl status taiga-mcp` (если используется systemd)

## Операции

### Обновление
1. Остановить сервис: `systemctl stop taiga-mcp` или Ctrl+C
2. Обновить код: `cd /opt/pytaiga-mcp && git pull`
3. Активировать venv: `source .venv/bin/activate`
4. Обновить зависимости: `uv pip install -r requirements.txt`
5. Запустить сервис: `systemctl start taiga-mcp` или `./run.sh --sse`

### Бэкап/восстановление
1. Бэкап проекта: `tar -czf pytaiga-mcp_$(date +%Y%m%d).tar.gz /opt/pytaiga-mcp`
2. Бэкап конфигурации: `cp /opt/pytaiga-mcp/.env /backup/`
3. Восстановление: `tar -xzf pytaiga-mcp_YYYYMMDD.tar.gz -C /opt/`
4. Восстановить права: `chown -R root:root /opt/pytaiga-mcp`
5. Пересоздать venv: `cd /opt/pytaiga-mcp && ./install.sh`

### Проверка после действий
- SSE endpoint отвечает и держит соединение
- Подключение к Taiga API работает
- Логи показывают успешную инициализацию
- MCP события передаются корректно

**Известные грабли:**
- "Тишина" в консоли — установить LOG_FILE=/dev/stdout в .env
- HTTP 307 при обращении к /sse/ — использовать /sse без конечного слэша
- --sse аргумент игнорируется — патч в src/server.py для парсера аргументов

**Обновлено:** 2024-12-19

---

### nextcloud-storage

**Назначение:** Облачное файловое хранилище и синхронизация

**Расположение:** 
- Контейнер: lxc-160-cloud
- URL: http://192.168.1.160 (внутренний)

**Интерфейсы/Порты:** 
- 80/HTTP — веб-интерфейс Nextcloud
- 443/HTTPS — защищённый доступ (если настроен)

**Данные:** 
- Данные Nextcloud: конфигурация и файлы пользователей
- MariaDB данные: база данных Nextcloud

**Зависимости:** 
- MariaDB (внутри контейнера)

**Конфигурация/переменные:** 
- MariaDB: ncuser / vault://nextcloud/db-password
- Admin: root / vault://nextcloud/admin-password
- Секреты: vault://nextcloud/credentials

**Мониторинг/здоровье:** 
- Проверка: `curl -f http://192.168.1.160`
- Статус служб внутри контейнера

## Операции

### Обновление
1. Создать бэкап данных и БД
2. Остановить службы Nextcloud
3. Обновить пакеты: `apt update && apt upgrade`
4. Запустить службы
5. Проверить веб-интерфейс

### Бэкап/восстановление
1. Остановить Nextcloud
2. Бэкап данных: `tar -czf nextcloud_data_$(date +%Y%m%d).tar.gz /var/www/nextcloud/data`
3. Бэкап БД: `mysqldump -u ncuser -p nextcloud > nextcloud_db_$(date +%Y%m%d).sql`
4. Восстановление данных: `tar -xzf nextcloud_data_YYYYMMDD.tar.gz -C /var/www/nextcloud/`
5. Восстановление БД: `mysql -u ncuser -p nextcloud < nextcloud_db_YYYYMMDD.sql`

### Проверка после действий
- Веб-интерфейс доступен и отвечает
- Пользователи могут войти в систему
- Файлы синхронизируются корректно
- База данных работает без ошибок

**Известные грабли:**
- N/A (требует дополнительной информации от пользователя)

**Обновлено:** 2024-12-19

---

## Шаблоны карточек

> Используйте эти шаблоны для создания новых карточек. Копируйте нужный шаблон и заполняйте поля.

### Шаблон ХОСТА

```markdown
### Хост — [Название]

**Назначение:** [краткое описание]

**Железо:** 
- CPU: [модель/количество ядер]
- RAM: [объем]
- Диски: [конфигурация]

**Сеть:** 
- [описание сетевой конфигурации]

**Хранилища:** 
- [список хранилищ и их назначение]

**Бэкапы VM/LXC:** 
- [инструмент, расписание, место хранения]

**Обслуживание:** 
- [регламент обслуживания]

**Известные грабли:**
- [список известных проблем]

**Обновлено:** YYYY-MM-DD
```

### Шаблон КОНТЕЙНЕРА/ВМ

```markdown
### lxc-<id>-<имя>

**Роль:** [назначение контейнера]

**Расположение:** 
- Хост: [название хоста]
- IP: [IP адрес]

**Ресурсы:** 
- CPU: [количество ядер]
- RAM: [объем памяти]
- Disk: [размер диска]

**Сеть/Порты:** 
- [список портов и их назначение]

**Данные/тома:** 
- [ключевые пути данных]

**Сервисы внутри:** 
- [список сервисов]

**Бэкап контейнера:** [способ и расписание бэкапа]

**Известные грабли:**
- [список известных проблем]

**Обновлено:** YYYY-MM-DD
```

### Шаблон СЕРВИСА/БОТА

```markdown
### [имя-сервиса]

**Назначение:** [краткое описание функции]

**Расположение:** 
- Контейнер: [название контейнера]
- URL: [если есть веб-интерфейс]

**Интерфейсы/Порты:** 
- [внешние точки входа]

**Данные:** 
- [где хранит состояние/файлы]

**Зависимости:** 
- [БД, очереди, API]

**Конфигурация/переменные:** 
- [путь к конфигу]
- Секреты: vault://[путь] или N/A

**Мониторинг/здоровье:** 
- [как проверить работоспособность]

## Операции

### Обновление
1. [шаг 1]
2. [шаг 2]
3. [шаг 3]
4. [проверка]

### Бэкап/восстановление
1. [шаг 1]
2. [шаг 2]
3. [шаг 3]
4. [проверка]

### Проверка после действий
- [что проверяем]
- [критерии успеха]

**Известные грабли:**
- [список известных проблем]

**Обновлено:** YYYY-MM-DD
```

---

## Глоссарий

- **Карточка** — минимальный стандартизированный блок описания объекта (хост/контейнер/сервис)
- **Runbook** — короткая пошаговая инструкция в карточке сервиса для типовых операций
- **Секреты** — пароли/ключи/токены; в справочнике **не храним**, только ссылки на хранилище
- **Источник правды** — единственное место, где хранится актуальная информация об объекте
- **Грабли** — известные проблемы и подводные камни, с которыми можно столкнуться

---

## Критерии готовности (Definition of Done)

- ✅ Разделы «Регламент», «Паспорт хоста», «Каталог контейнеров», «Каталог сервисов» и «Шаблоны» присутствуют и заполнены
- ✅ На каждый существующий контейнер и сервис создана **карточка** по шаблону (поля заполнены или помечены «N/A»)
- ✅ Во всех карточках есть поле **«Обновлено: YYYY-MM-DD»** с текущей датой
- ✅ В карточках **нет секретов**; есть ссылки-указатели на место хранения секретов (если применимо)
- ✅ Все процедуры в разделе «Операции» укладываются в 3–7 коротких шагов и содержат пункт «Проверка после действий»

## Правила сопровождения

- При любом изменении портов/доменов/путей/зависимостей — правка соответствующей карточки в день изменения
- Если во время работ добавлен новый шаг — добавить его в «Операции»
- Если найден «грабельный» случай — добавить краткий пункт в «Известные грабли»
- Дата обновления карточки должна соответствовать дате последнего изменения

---

*Справочник создан: 2024-12-19*  
*Версия: 1.0*
